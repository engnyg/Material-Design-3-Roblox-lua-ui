Images used by `examples/executor.lua`.

`extension.png` is the Material Icons "extension" glyph (Outlined), rendered
white on transparency from `../fonts/MaterialIconsOutlined-Regular.ttf`
(Apache License 2.0, see `../fonts/LICENSE`). Icon images must be white so
`ImageColor3` can tint them with the theme color.
